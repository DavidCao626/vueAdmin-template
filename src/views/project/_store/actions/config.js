import api from '../../_api/config.js'

const configGet = ({ commit, state }, projectId) =>
  new Promise(resolve => {
   // commit("setProjectInfoData",{1\\});
    console.log('doit')
  })

export default {
  configGet
}
